const PI = 3.14;

function add(x, y) {
    return x + y;
}

function subtract(x, y) {
    return x - y;
}

const Math = {
    PI,
    add,
    subtract
};

export { PI, add, subtract };
export default Math;