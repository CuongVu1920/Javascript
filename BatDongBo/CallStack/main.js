/**
 * Callstack trong JS là 1 cấu trúc dữ liệu ngăn xếp (stack) dùng để quản lý thứ tự
 * thực thi các hàm. Khi một hàm được gọi, nó được đẩy vào call stack;
 * - khi hàm hoàn thành, nó được lấy ra khỏi stack.
 * - quá trình này giúp JS biết hàm nào đang chạy, hàm nào sẽ chạy tiếp theo và kiểm tra luồng thực thi mã.
 * 
 * => Call stack giúp JavaScript thực hiện cơ chế đồng bộ (synchronous) và xác định thứ tự thực thi các hàm trong chương trình.
 */
